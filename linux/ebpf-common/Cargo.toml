[package]
name = "secureexec-ebpf-common"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.13", optional = true }

[lib]
path = "src/lib.rs"
